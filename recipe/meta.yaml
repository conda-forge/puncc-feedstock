{% set name = "puncc" %}
{% set version = "0.9.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/puncc-{{ version }}.tar.gz
  sha256: 26a177ce45fe9f7ade6938288b01ad1fc7a8d6df773560fb01bfdbdfbadd96de

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
    - setuptools_scm
  run:
    - python >={{ python_min }}
    - joblib
    - matplotlib-base
    - numpy <2.0.0
    - pandas
    - scikit-learn >=1.5
    - tqdm

test:
  imports:
    - deel.puncc
    - deel.puncc.api
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/deel-ai/puncc
  summary: Predictive UNcertainty Calibration and Conformalization Library
  license_file: MIT-Clause.txt
  license: MIT

extra:
  recipe-maintainers:
    - M-Mouhcine
    - CyrilJl
